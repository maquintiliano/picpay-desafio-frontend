import { AppTableBodyDirective } from './app-table-body.directive';

describe('AppTableBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new AppTableBodyDirective();
    expect(directive).toBeTruthy();
  });
});
